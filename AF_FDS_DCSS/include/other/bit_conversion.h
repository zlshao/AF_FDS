#ifndef BIT_CONVERSION_H
#define BIT_CONVERSION_H

#include <string>
#include <stdint.h>

uint32_t getStringValue(std::string str);

uint64_t convStringValue(std::string str);

int calSampleRate(int num);

#endif
